apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - namespace.yaml

namespace: discord-translator-preprod

commonLabels:
  environment: preprod

patchesStrategicMerge:
  - patches.yaml
