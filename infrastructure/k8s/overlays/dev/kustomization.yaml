apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - namespace.yaml

namespace: discord-translator-dev

commonLabels:
  environment: dev

patchesStrategicMerge:
  - patches.yaml
